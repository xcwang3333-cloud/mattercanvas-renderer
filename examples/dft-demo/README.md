# DFT figure demo fixtures

These files are synthetic software fixtures for testing the MatterCanvas multi-panel VASP figure pipeline. They are **not** intended to represent validated physical structures or scientific results.

Select all four `.vasp` files in `demo/` to smoke-test automatic ordering, Nature/JACS presets, unit-cell rendering, SVG export, and high-resolution PNG export.
